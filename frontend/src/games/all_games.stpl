<div class="grid game-grid" id="games">
  <% let editing = true; let user_id = 0; let game = GameModel::default(); %>
  <% include!("./game.stpl"); %>
  <% let editing = false; let user_id = self.user_id; %>
  <% for game in self.games { %>
    <% include!("./game.stpl"); %>
  <% } %>
</div>
